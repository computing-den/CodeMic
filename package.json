{
  "name": "CodeCast",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm run build && code --extensionDevelopmentPath=$PWD/packages/vscode-extension",
    "server": "npm run build && npm run kill:server && node --enable-source-maps packages/webserver/out/server.js",
    "dev:server": "npm run build && npm run kill:server && node --enable-source-maps --inspect=9230 packages/webserver/out/server.js",
    "kill:server": "node  kill-server.mjs",
    "build": "npm run build:tsc && npm run build:webview && npm run build:webclient",
    "build:tsc": "tsc --build",
    "watch:tsc": "tsc --build --watch --pretty false",
    "build:webview": "cd packages/vscode-webview && node esbuild.mjs",
    "build:webclient": "cd packages/webclient && node esbuild.mjs",
    "clean": "rm -rf packages/*/out && rm -rf packages/*/tsconfig.tsbuildinfo"
  },
  "workspaces": [
    "packages/vscode-extension",
    "packages/vscode-webview",
    "packages/webserver",
    "packages/webclient",
    "packages/lib"
  ],
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
